import { Home, BookOpen, Code, Terminal, Zap, Cloud, Github, KeyRound, Rocket, FlaskConical, User, LayoutGrid } from 'lucide-react';

export const icons = {
  Home,
  BookOpen,
  Code,
  Terminal,
  Zap,
  Cloud,
  Github,
  KeyRound,
  Rocket,
  FlaskConical,
  User,
  LayoutGrid,
};

export type IconName = keyof typeof icons;